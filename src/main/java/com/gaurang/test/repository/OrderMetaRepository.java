package com.gaurang.test.repository;

import com.gaurang.test.entity.OrderMeta;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OrderMetaRepository extends JpaRepository<OrderMeta,Integer> {
}
